{
    "title": "Vizcom 3D Generator",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "imageUrl": {
            "title": "Image URL (optional)",
            "type": "string",
            "description": "Direct URL to an image to upload instead of the bundled leg.png.",
            "editor": "textfield"
        },
        "prompt": {
            "title": "Prompt (optional)",
            "type": "string",
            "description": "Prompt text used to adjust or generate the image in Vizcom.",
            "editor": "textfield"
        },
        "headed": {
            "title": "Run Headed (debug)",
            "type": "boolean",
            "description": "If true, the browser will run in headed mode for local debugging.",
            "default": false
        }
    },
    "required": []
}
